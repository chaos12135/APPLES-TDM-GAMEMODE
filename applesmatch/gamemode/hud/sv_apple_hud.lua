// SERVE SIDE

-- // Restarts the server if the timer reaches SPECIFIC TIME
-- timer.Create("testtest2",1,0, function()
-- if tonumber(600 - math.Round(CurTime())) <= 0 then
-- MsgN("test")
-- game.ConsoleCommand("changelevel "..game.GetMap( ).."\n")
-- end
-- MsgN(600 - math.Round(CurTime()))
-- end)
